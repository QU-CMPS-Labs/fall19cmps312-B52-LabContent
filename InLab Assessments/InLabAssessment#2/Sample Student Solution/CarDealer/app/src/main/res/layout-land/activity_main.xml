<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/region_image"
        android:layout_width="@dimen/flag_width_land"
        android:layout_height="@dimen/flag_height_land"
        android:layout_marginStart="52dp"
        android:layout_marginTop="48dp"
        android:src="@drawable/branch_flag"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/region_name"
        app:layout_constraintVertical_bias="0.0" />

    <ImageView
        android:id="@+id/car_image"
        android:layout_width="@dimen/car_width_land"
        android:layout_height="@dimen/car_height_land"
        android:layout_marginTop="67dp"
        android:layout_marginEnd="32dp"
        android:layout_marginBottom="72dp"
        android:contentDescription="TODO"
        android:scaleType="fitXY"
        android:src="@drawable/car"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.484"
        app:layout_constraintStart_toEndOf="@+id/region_image"
        app:layout_constraintTop_toBottomOf="@+id/region_name" />

    <TextView
        android:id="@+id/region_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:fontFamily="@font/bilbo_swash_caps"
        android:text="@string/best_luxury_car_dealer_united_kingdom_branch"
        android:textAlignment="center"
        android:textSize="36sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.47"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>